{
  "name": "eventcatalog",
  "version": "1.0.0",
  "scripts": {
    "dev": "eventcatalog dev",
    "build": "eventcatalog build",
    "generate": "eventcatalog generate",
    "lint": "spectral lint ../api/*.yaml",
    "generate:go": "echo 'Running asyncapi-codegen...' && # asyncapi-codegen -g gopassthrough -i ../api/order-service.yaml -o ../internal/events/generated.go"
  },
  "dependencies": {
    "@eventcatalog/core": "latest",
    "@eventcatalog/plugin-doc-generator-asyncapi": "latest"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.15.1"
  }
}
