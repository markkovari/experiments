apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090 # Prometheus service runs on port 9090 within the docker network
    access: proxy
    isDefault: true
    editable: true