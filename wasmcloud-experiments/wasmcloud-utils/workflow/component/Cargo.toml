[package]
name = "workflow-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
workflow-core = { path = "../core" }

[package.metadata.component]
package = "wasmcloud:workflow"

[package.metadata.component.target]
path = "../../wit/wasmcloud-workflow"
