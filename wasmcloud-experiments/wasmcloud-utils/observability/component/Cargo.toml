[package]
name = "observability-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
observability-core = { path = "../core" }

[package.metadata.component]
package = "wasmcloud:observability"

[package.metadata.component.target]
path = "../../wit/wasmcloud-observability"
