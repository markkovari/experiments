// Step definitions specific to step output retrieval.
// The core GET step and check steps are handled by common.rs.
// execution_steps.rs provides "I mark step X as done" Given steps.
// This module is intentionally minimal — all required steps are covered
// by the shared modules.
