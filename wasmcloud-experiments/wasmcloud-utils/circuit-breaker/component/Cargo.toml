[package]
name = "circuit-breaker-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
circuit-breaker-core = { path = "../core" }

[package.metadata.component]
package = "wasmcloud:circuit-breaker"

[package.metadata.component.target]
path = "../../wit/wasmcloud-circuit-breaker"
