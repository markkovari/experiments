apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: cron
  annotations:
    version: v0.1.0
    description: "Cron schedule registry component"
spec:
  components:
    - name: cron-component
      type: component
      properties:
        image: file://../target/wasm32-wasip2/release/cron_component.wasm
      traits:
        - type: spreadscaler
          properties:
            replicas: 1
