# Terraform usage of digital ocean


Variables:
envvars:

`AWS_SECRET_ACCESS_KEY` and `AWS_ACCESS_KEY_ID` for the terraform state [backend](./backend.tf)

other variables look at the [variables terraform file](./variables.tf)