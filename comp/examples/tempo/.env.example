# Strong password for the local Valkey/Redis (required).
REDIS_PASSWORD=change-me-to-a-long-random-string

# On a droplet, run a prebuilt image instead of building locally:
# TEMPO_IMAGE=ghcr.io/<you>/tempo-app:0.1.0
